<% provide(:title, @instructor.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <% if @instructor.image? %>                                                              
        <%= image_tag @instructor.image.url %>
        <% end %>
        <%= @instructor.name %>
      </h1>
    </section>
  </aside>
</div>